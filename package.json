{
  "name": "spilltrace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Oil Spill Detection & Vessel Attribution Maritime Dashboard",
  "scripts": {
    "dev": "vite",
    "backend": "python -m uvicorn backend.main:app --reload --port 8000",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@turf/turf": "^7.1.0",
    "maplibre-gl": "^6.6.0",
    "puppeteer": "^25.9.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
